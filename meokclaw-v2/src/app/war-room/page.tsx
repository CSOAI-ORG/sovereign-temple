import WarRoom from "@/components/war-room";

export const metadata = {
  title: "War Room — MEOKCLAW v2",
  description: "OpenClaw Visual Workspace. Real-time hemisphere activity, model nodes, cost burn, and router health.",
};

export default function WarRoomPage() {
  return <WarRoom />;
}
